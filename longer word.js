// tells you which word is longer word
function longer(a, b) {
    if (a.length > b.length) {
        return a;
    } else {
        return b;
    }
};
console.log(longer('hallo', 'ja'));
// works